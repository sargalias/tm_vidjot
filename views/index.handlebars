
<div class="jumbotron text-center">
    <h1 class="display-3">{{title}}</h1>
    <p class="lead">Jot down ideas for your next YouTube videos</p>
    {{#if user}}
        <a href="/ideas/new" class="btn btn-info btn-lg">Add video idea</a>
    {{else}}
        <a href="/users/login" class="btn btn-info btn-lg">Login</a>
    {{/if}}
</div>